# Engineering

Skills for daily code work.

## User-invoked

Reachable only when you type them (Claude Code: `disable-model-invocation: true`; Codex: `policy.allow_implicit_invocation: false` in `agents/openai.yaml`).

- **[submit-review](./submit-review/SKILL.md)** — Publish an in-session code review to a GitHub PR as staged (pending) inline comments, or as replies to existing review threads. Terse by default; verbosity set by the argument.

## Model-invoked

Model- or user-reachable (rich trigger phrasing so the model can reach for them).

_None yet._
