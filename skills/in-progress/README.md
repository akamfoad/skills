# In progress

Drafts not yet ready to ship — **not promoted**. Promote by moving the skill into `engineering/` or `productivity/`, then add it to the top-level `README.md` and `.claude-plugin/plugin.json`.
